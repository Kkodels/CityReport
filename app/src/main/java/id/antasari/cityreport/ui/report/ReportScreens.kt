package id.antasari.cityreport.ui.report

import androidx.compose.runtime.Composable

/**
 * Jika sebelumnya file ini berisi versi lama dari screen laporan
 * (dengan SmallTopAppBar, dll), kita sederhanakan agar tidak
 * bentrok dengan file ReportFormScreen / ReportListScreen / ReportDetailScreen
 * yang baru.
 *
 * Kalau mau, kamu bisa pakai file ini untuk kumpulan preview saja.
 */

@Composable
fun ReportScreensPreviewEntry() {
    // Contoh: panggil salah satu screen untuk Preview
    // ReportFormScreen(onBack = {}, onReportSubmitted = {})
}
